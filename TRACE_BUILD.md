# Build trace: which files are used (DX12 backend)

Analysis of both build configs. Extracted from the generated `.vcxproj` compile
lists (authoritative: every file the build compiles) + the CMake `DX12_SHADERS`
list for `.hlsl`.

Generated by `bench_results/trace_report.py`.

## Configs

| Config | Build dir | DX12_AGILITY | vcxproj source entries |
|--------|-----------|--------------|------------------------|
| agility ON | `build_dx12_agility` | ON | 636 |
| agility OFF | `build_dx12` | OFF | 649 |

Both use the same source tree; the small count difference is the agility
conditional (e.g. agility shim / D3D12Core copy target).

## Totals

- Source files on disk (scanned): 1314
- Used by agility build: 516
- Used by non-agility build: 523
- Used by at least one build: 524

## NOT used by any build - cleanup

### Fork-specific dead files (DELETED this session)

- `ggml/src/ggml-dx12/build/` - nested cmake build dir inside the source tree
- `ggml/src/ggml-dx12/generated/` - stale pre-refactor shader registry + .cso
  (current build compiles .hlsl -> .cso into the build dir)
- `ggml/src/ggml-dx12/shaders/fused_gemm_add_q4k.hlsl` - not in DX12_SHADERS
- `ggml/src/ggml-dx12/shaders/wg_scale.hlsl` - not in DX12_SHADERS (workgraph-only)
- `ggml/src/ggml-dx12/shaders/DX12_AI_PIPELINE.hlsli` - unreferenced
- `ggml/src/ggml-dx12/tests/test_dx12_registry.cpp` - not in tests/CMakeLists
- `ggml/src/ggml-dx12/tests/test_minimal_dispatch.cpp` - not in tests/CMakeLists
- `src/models/dspark.cpp` + all dspark/markov refs - removed earlier (crashing)

### KEEP (upstream llama.cpp, other backends - not built by DX12 but part of the codebase)

The bulk of the "unused" list is the multi-backend llama.cpp source: `ggml-cuda/`,
`ggml-metal/`, `ggml-vulkan/`, `ggml-sycl/`, `ggml-opencl/`, `ggml-openvino/`,
`ggml-hexagon/`, `ggml-virtgpu/`, `ggml-webgpu/`, `ggml-zdnn/`, `ggml-zendnn/`,
`ggml-rpc/`, `ggml-blas/`, `ggml-cann/`, `ggml-musa/`, CPU arch dirs
(`arch/arm|riscv|powerpc|s390|wasm|loongarch|spacemit`), `kleidiai/`, `amx/`,
and the non-built example tools. These are upstream llama.cpp and should stay.

### Headers

Many headers appear "unused" because they are pulled in via `#include`, not
listed in `.vcxproj`. The header list in the trace is a false positive - do not
delete headers on this trace alone.

## How to regenerate

```
python bench_results/trace_report.py
```

Uses `build_dx12_agility` and `build_dx12` `.vcxproj` files as the source of truth.
